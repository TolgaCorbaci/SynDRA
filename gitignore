# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# Local lock (commit deliberately if you want exact pins)
# requirements-lock.txt

# Large/intermediate data — keep only released, versioned outputs
*.tmp

# Data dumps
synonyms/input/Drugcentral/drugcentral.dump.11012023.sql